#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    bool isNegative = false;
    if (n < 0) {
        isNegative = true;
        n = -n;
    }//判断输入的数是否是负数，以便最后一步翻转时加符号
    int reversed = 0;
    while (n != 0) {
        int digit = n % 10;
        reversed = reversed * 10 + digit;
        n /= 10;
    }
    if (isNegative) {
        reversed = -reversed;
    }
    cout << reversed << endl;
    return 0;
}
