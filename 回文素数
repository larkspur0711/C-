//回文素数
#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    int a, b;
    cin >> a >> b;
  
    if (a <= 5 && 5 <= b) {
        cout << 5 << endl;
    }
    if (a <= 7 && 7 <= b) {
        cout << 7 << endl;
    }
    if (a <= 11 && 11 <= b) {
        cout << 11 << endl;
    }

    for (int d1 = 1; d1 <= 9; d1 += 2) {
        for (int d2 = 0; d2 <= 9; d2++) {
            int palindrome = 100 * d1 + 10 * d2 + d1;
            if (palindrome >= a && palindrome <= b) {
                bool isPrime = true;
                for (int i = 2; i <= static_cast<int>(sqrt(static_cast<double>(palindrome))); ++i) {
                    if (palindrome % i == 0) {
                        isPrime = false;
                        break;
                    }
                }
                if (isPrime) {
                    cout << palindrome << endl;
                }
            }
        }
    }

    for (int d1 = 1; d1 <= 9; d1 += 2) {
        for (int d2 = 0; d2 <= 9; d2++) {
            for (int d3 = 0; d3 <= 9; d3++) {
                int palindrome = 10000 * d1 + 1000 * d2 + 100 * d3 + 10 * d2 + d1;
                if (palindrome >= a && palindrome <= b) {
                    bool isPrime = true;
                    for (int i = 2; i <= static_cast<int>(sqrt(static_cast<double>(palindrome))); ++i) {
                        if (palindrome % i == 0) {
                            isPrime = false;
                            break;
                        }
                    }
                    if (isPrime) {
                        cout << palindrome << endl;
                    }
                }
            }
        }
    }

    for (int d1 = 1; d1 <= 9; d1 += 2) {
        for (int d2 = 0; d2 <= 9; d2++) {
            for (int d3 = 0; d3 <= 9; d3++) {
                for (int d4 = 0; d4 <= 9; d4++) {
                    int palindrome = 1000000 * d1 + 100000 * d2 + 10000 * d3 + 1000 * d4 + 100 * d3 + 10 * d2 + d1;
                    if (palindrome >= a && palindrome <= b) {
                        bool isPrime = true;
                        for (int i = 2; i <= static_cast<int>(sqrt(static_cast<double>(palindrome))); ++i) {
                            if (palindrome % i == 0) {
                                isPrime = false;
                                break;
                            }
                        }
                        if (isPrime) {
                            cout << palindrome << endl;
                        }
                    }
                }
            }
        }
    }

    return 0;
}
